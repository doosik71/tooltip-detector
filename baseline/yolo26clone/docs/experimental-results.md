# YOLO26 클론 실험결과 분석 보고서

> 대상: [baseline/yolo26clone](../README.md). YOLO26을 `ultralytics` 없이 순수 PyTorch로
> 재구현하고, 이 저장소의 어노테이션으로 **무작위 초기화에서부터** 학습한 것이다.
> 이하 **YOLO26 클론**으로 부르며, COCO 사전학습 가중치를 파인튜닝하는
> [baseline/yolo26](../../yolo26/README.md)(이하 **참조 구현**, 보고서는
> [experimental-results.md](../../yolo26/docs/experimental-results.md))과는 다른 모델이다.
>
> 이 문서의 수치는 별도 표시가 없는 한 [summary-results.md](summary-results.md)에 있고,
> 그 문서는 `scripts/generate-summary.py`가 `data/model/`·`data/results/`에서 다시 계산한다.
> 비교 대상의 수치도 각 서브 프로젝트의 같은 이름 문서에서 가져왔다. 예외는 §8의
> 보조 측정뿐이며, 그 절에 측정 조건을 밝혔다.

## 요약

- **재구현이 실제로 작동한다.** 사전학습 없이 스크래치 학습만으로 참조 구현에
  근접했다. erop test에서 mAP@0.5 0.8445 대 0.8476(차이 0.003), cholec80 test에서
  0.7247 대 0.7348(차이 0.010)이다. 앞서 `verify-clone`이 확인한 것은 "같은 것을
  계산하고 같은 것을 학습한다"까지였고, 이 실험이 확인하는 것은 **그렇게 학습하면 실제로
  같은 수준에 도달한다**는 것이다.
- **남은 격차는 탐지 실패율 한 곳에 몰려 있다.** cholec80에서 9.71 % 대 7.45 %로 2.26 pp
  뒤진다. 반대로 헝가리안 정밀도는 0.7279 대 0.7120으로 앞선다. 클론은 예측을 덜 내고
  덜 맞히는 쪽에, 참조 구현은 더 내고 더 맞히는 쪽에 서 있다 (§6).
- **같은 학습 루프로 학습한 YOLOv8s 클론과 비교하면 AP@0.5에서 지고 AP@0.5:0.95에서
  이긴다.** 두 데이터셋 모두 방향이 같다. 느슨한 IoU에서는 NMS가 있는 쪽이, 엄격한
  IoU에서는 DFL 없이 거리를 직접 회귀하는 쪽이 유리하다는 해석과 맞는다 (§7).
- **팁 좌표 정밀도는 재구현 베이스라인 중 가장 높다.** 오차 중앙값 cholec80 3.45 px,
  erop 1.96 px로 YOLOv8s 클론(3.60 px, 2.04 px)·CLAD-Net(4.93 px, 3.23 px)보다 작다.
- **속도는 기대와 반대다.** NMS가 없는데도 같은 GPU에서 YOLOv8s 클론의 1.65배 시간이
  걸린다. 그중 15 %는 추론에서 쓰지도 않는 one2many 분기를 계산하는 데 쓰이고 있다.
  코드 한 줄로 회수할 수 있는 낭비다 (§8).
- **최대 약점은 여전히 다중 도구 프레임의 과소 탐지다.** 200 px 이상 오차의 87〜88 %가
  "위치를 틀린 것"이 아니라 "도구 하나를 통째로 놓친 것"이다. 네 베이스라인과 루트
  프로젝트가 모두 같은 문제를 안고 있다.

## 1. 실험 설정

| 항목             | 값                                                                                         |
| ---------------- | ------------------------------------------------------------------------------------------ |
| 아키텍처         | YOLO26-s: C3k2 백본 + SPPF + C2PSA + PAN-FPN + end-to-end 앵커프리 헤드                    |
| 헤드             | one2many(학습 전용) + one2one(추론) 두 분기. one2one은 detach된 특징을 받는다              |
| 파라미터         | 9,949,412 (2클래스). `nc=80`으로 세면 10,009,784로 참조 체크포인트와 완전 일치             |
| 클래스           | `tool`(어노테이션 bbox), `tip`(팁 좌표 중심 **32 × 32 px** 박스)                           |
| 입력             | 640 × 640 letterbox (원본 736 × 480)                                                       |
| 라벨 할당        | TaskAlignedAssigner + 박스 크기 하한(stride) + 2차 top-k. one2many topk 10, one2one topk 7 |
| 손실             | `w·L_one2many + (1−w)·L_one2one`, 각 분기 `7.5·CIoU + 0.5·BCE(cls) + 1.5·L1`               |
| 손실 가중치 `w`  | 0.8 → 0.1 **선형 감쇠**. 보고되는 손실은 one2one 분기의 것                                 |
| DFL              | **없음** (`reg_max=1`). 분포가 아니라 네 거리 값을 직접 회귀한다                           |
| 후처리           | **NMS 없음.** top-k 300개만 남긴다                                                         |
| 에포크           | **150**                                                                                    |
| 배치 / lr        | 16 / 0.01 (**MuSGD**, momentum 0.9, nesterov, 분류 헤드 lr 3배)                            |
| 증강             | mosaic + 좌우 반전 + HSV 지터                                                              |
| 추가             | warmup 3 에포크, cosine 감쇠, 가중치 EMA, 그래디언트 클리핑 10                             |
| `--frame-stride` | **5** (학습 프레임 5장 중 1장)                                                             |
| `--val-frames`   | 1,500                                                                                      |
| 사전학습         | **없음** (무작위 초기화)                                                                   |

증강·warmup·cosine 감쇠·EMA·클리핑·`--frame-stride`는 [CLAD-Net](../../cladnet/README.md),
[YOLOv8s 클론](../../yolov8sclone/README.md)과 **동일하게 맞췄다.** 세 재구현 베이스라인의
차이가 모델·손실·할당·옵티마이저의 차이이도록 하려는 선택이다. 반대로 참조 구현과는
학습 루프가 다르다: 참조는 linear 감쇠에 nominal batch 64를 맞춘 그래디언트 누적을 쓴다.

### 데이터

| 데이터셋   | train (stride 5 적용) |                          val |   test | test GT 팁 |          test 세션 |
| ---------- | --------------------: | ---------------------------: | -----: | ---------: | -----------------: |
| `cholec80` |       71,032 → 14,207 | 15,312 (에포크당 1,500 표본) | 98,234 |    161,969 | 40편 (video41〜80) |
| `erop`     |      108,424 → 21,685 | 36,140 (에포크당 1,500 표본) | 36,142 |     50,718 |              5세션 |

(train/val 프레임 수는 `data/dataset/`을 읽어야 나오는 값이라 `summary-results.md`에 없다.
같은 데이터셋·같은 stride로 학습한 [YOLOv8s 클론 보고서](../../yolov8sclone/docs/experimental-results.md) §1에서 가져왔다.)

cholec80은 **영상 단위로 분리된** 스플릿(video01〜32 train / 33〜40 val / 41〜80 test)이다.
erop은 영상 내부를 프레임 단위로 섞어 나눈 스플릿이라 인접 프레임이 train과 test에 함께
들어간다(시간적 누수). **erop 수치는 cholec80보다 낙관적으로 읽어야 한다.**

## 2. 학습 경과

| 에포크 | cholec80 val loss |   mAP@0.5 | tool AP50 |  tip AP50 | erop val loss |   mAP@0.5 | tool AP50 |  tip AP50 |
| -----: | ----------------: | --------: | --------: | --------: | ------------: | --------: | --------: | --------: |
|      1 |            13.758 |     0.001 |     0.001 |     0.000 |        16.919 |     0.000 |     0.001 |     0.000 |
|      2 |             4.234 | **0.399** |     0.595 | **0.204** |         3.191 | **0.589** |     0.779 | **0.400** |
|      5 |             3.200 |     0.550 |     0.685 |     0.414 |         2.073 |     0.761 |     0.898 |     0.624 |
|     10 |             2.692 |     0.626 |     0.765 |     0.487 |         1.730 |     0.817 |     0.923 |     0.712 |
|     30 |             2.448 |     0.674 |     0.796 |     0.552 |         1.607 |     0.844 |     0.930 |     0.758 |
|     50 |             2.400 |     0.694 |     0.803 |     0.585 |         1.526 |     0.861 |     0.932 |     0.790 |
|     75 |         **2.347** |     0.710 |     0.809 |     0.610 |         1.449 |     0.873 |     0.933 |     0.814 |
|    100 |             2.358 |     0.722 | **0.815** |     0.629 |     **1.412** |     0.880 |     0.929 |     0.830 |
|    125 |             2.456 | **0.725** |     0.812 |     0.638 |         1.421 |     0.884 |     0.928 | **0.840** |
|    150 |             2.568 |     0.723 |     0.804 | **0.641** |         1.454 | **0.885** | **0.933** |     0.837 |

**수렴했고, 끝에서 과적합이 시작됐다.**

| 데이터셋   |   최고 에포크 | 최고 mAP@0.5:0.95 | 150 에포크 값 |             학습 시간 |
| ---------- | ------------: | ----------------: | ------------: | --------------------: |
| `cholec80` | **107** / 150 |            0.5433 |        0.5269 | 5.8 h (2.3 분/에포크) |
| `erop`     | **123** / 150 |            0.7229 |        0.7154 | 8.2 h (3.3 분/에포크) |

val loss는 cholec80 75 에포크(2.347), erop 100 에포크(1.412)에서 바닥을 치고 다시 오른다.
mAP@0.5는 그 뒤로도 조금 더 오르지만 mAP@0.5:0.95는 최고점 이후 하락한다. **이 설정에서
150 에포크는 필요 이상이며 110〜125 에포크에서 끊어도 같은 결과를 얻는다.** `model.pt`는
최고점에서 저장되므로 보고된 성능은 영향을 받지 않는다. 최고점이 YOLOv8s 클론(108 / 125)과
거의 같은 지점인 것은 데이터 소진 시점이 모델이 아니라 학습 프레임 수(14,207·21,685장)로
정해진다는 뜻으로 읽힌다.

### 사전학습이 지우는 것은 초반 20 에포크다

같은 아키텍처를 COCO 가중치에서 시작한 참조 구현과 나란히 놓으면 사전학습의 몫이 보인다.

| cholec80 mAP@0.5          |      1 ep |      2 ep |      5 ep |     10 ep |     20 ep |     50 ep | 100 ep |      최고 |
| ------------------------- | --------: | --------: | --------: | --------: | --------: | --------: | -----: | --------: |
| YOLO26 클론 (스크래치)    |     0.001 |     0.399 |     0.550 |     0.626 |     0.663 |     0.694 |  0.722 |     0.726 |
| 참조 구현 (COCO 파인튜닝) | **0.606** | **0.656** | **0.661** | **0.705** | **0.749** | **0.775** |  0.771 | **0.777** |

참조 구현은 **1 에포크 만에** 클론이 20 에포크에 도달하는 지점을 지난다. 그러나 클론이
100 에포크에서 0.722까지 올라오면서 격차는 0.05로 줄고, test 전수 평가에서는 0.010까지
좁혀진다(§3). **사전학습은 수렴을 크게 앞당기지만 이 데이터에서 도달 가능한 상한을 크게
바꾸지는 않는다.**

또 하나 눈에 띄는 것은 참조 구현이 **3 에포크에서 한 번 무너진다**는 점이다
(mAP@0.5 0.656 → 0.608, erop에서는 val cls가 1.21 → 2.53). warmup이 끝나 학습률이
최고값에 닿는 지점이며, 사전학습 가중치가 그 충격을 한 번 받는다. 무작위 초기화에서
시작하는 클론에는 이 굴곡이 없다.

## 3. 탐지 성능 (test 전수)

`conf=0.25`, `max_det=300`. AP 곡선은 `conf=0.001`까지 내려가며 계산했다. 이 헤드는
end-to-end라 NMS가 없고, 따라서 IoU 임계값도 없다.

| 데이터셋   | mAP@0.5 | mAP@0.5:0.95 | tool AP@0.5 | tool AP@0.5:0.95 | tip AP@0.5 | tip AP@0.5:0.95 |
| ---------- | ------: | -----------: | ----------: | ---------------: | ---------: | --------------: |
| `cholec80` |  0.7247 |       0.5548 |      0.8172 |           0.6948 |     0.6321 |          0.4148 |
| `erop`     |  0.8445 |       0.6921 |      0.8894 |           0.8055 |     0.7996 |          0.5787 |

`tip` AP@0.5가 0.63·0.80으로 `tool`(0.82·0.89)에 크게 뒤지지 않는다. 팁 박스를 32 px로
키운 효과다. 32 px은 letterbox 후 27.8 px이라, assigner의 박스 크기 하한(stride 16 px)에
걸리지 않는다. 이 하한에 걸리는 크기(10 px 등)로 줄이면 팁 GT가 억지로 16 px로 늘어난
상태로 앵커를 배정받게 되므로, 32 px 선택은 이 모델에서 특히 의미가 있다.

> **주의**: `summary.json`의 클래스별 `precision`(tool 0.198, tip 0.055)은 AP 곡선을
> 그리기 위한 `conf=0.001`에서 누적된 값이라 운용 지점의 정밀도가 아니다.
> 운용 지점은 §4의 헝가리안 표다.

## 4. 팁 위치 정확도: 다른 모델들과의 비교

**같은 test 프레임, 같은 GT 팁, 같은 매칭 규칙**으로 측정했다
(GT 팁 수가 cholec80 161,969 / erop 50,718로 모든 모델에서 정확히 일치한다).

### cholec80 (test, GT 팁 161,969)

| 모델                            | 탐지 실패율 |   Hit@10 px |   Hit@20 px |   Hit@50 px |      중앙값 |         평균 |           P90 |
| ------------------------------- | ----------: | ----------: | ----------: | ----------: | ----------: | -----------: | ------------: |
| **YOLO26 클론**                 |      9.71 % |     59.27 % |     64.51 % |     70.45 % | **3.45 px** |     46.12 px |     186.85 px |
| 참조 구현 (사전학습)            |      7.45 % | **61.57 %** |     67.02 % |     73.31 % |     3.31 px |     43.82 px |     177.14 px |
| YOLOv8s 클론                    |      8.82 % |     59.87 % |     65.49 % |     71.78 % |     3.60 px |     44.66 px |     181.08 px |
| CLAD-Net                        |      9.56 % |     56.65 % |     63.12 % |     69.96 % |     4.93 px |     46.73 px |     184.63 px |
| tooltip-detector `gaussian-tip` |      7.48 % |     59.55 % | **67.45 %** |     74.72 % |     5.39 px | **42.37 px** |     167.34 px |
| tooltip-detector `gradient-seg` |  **2.90 %** |     30.04 % |     55.58 % | **76.12 %** |    16.12 px |     48.41 px | **156.66 px** |

(참조 구현을 볼드에서 뺀 것은 그 모델만 COCO 사전학습을 쓰기 때문이다. 볼드는 스크래치
학습 모델 사이의 최고값이다. 이하 같다.)

### erop (test, GT 팁 50,718)

| 모델                            | 탐지 실패율 |   Hit@10 px |   Hit@20 px |   Hit@50 px |      중앙값 |         평균 |          P90 |
| ------------------------------- | ----------: | ----------: | ----------: | ----------: | ----------: | -----------: | -----------: |
| **YOLO26 클론**                 |      5.96 % |     74.25 % |     77.38 % |     80.93 % | **1.96 px** |     30.29 px |    112.90 px |
| 참조 구현 (사전학습)            |      5.65 % |     74.59 % |     77.50 % |     81.06 % |     1.90 px |     30.44 px |    116.87 px |
| YOLOv8s 클론                    |  **4.79 %** | **75.91 %** |     79.28 % |     83.16 % |     2.04 px | **27.82 px** | **91.81 px** |
| CLAD-Net                        |      6.83 % |     68.46 % |     72.76 % |     77.02 % |     3.23 px |     38.04 px |    159.16 px |
| tooltip-detector `gaussian-tip` |      5.03 % |     69.52 % | **79.36 %** | **83.82 %** |     7.81 px |     29.85 px |     74.28 px |
| tooltip-detector `gradient-seg` |      3.09 % |     44.98 % |     70.17 % |     82.95 % |    10.77 px |     34.94 px |     88.68 px |

### 헝가리안 1:1 매칭 (운용 지점)

프레임마다 예측과 GT를 1:1로 최적 매칭한 값이다. 여러 GT가 한 예측을 나눠 갖는 일이
없으므로, 위 표가 감추는 과소 탐지가 FN으로 드러난다.

| 데이터셋 / 모델            | cap   |     TP |     FP |     FN | Precision |    Recall |
| -------------------------- | ----- | -----: | -----: | -----: | --------: | --------: |
| cholec80 / **YOLO26 클론** | 10 px | 95,948 | 35,868 | 66,021 |     0.728 |     0.592 |
| cholec80 / 참조 구현       | 10 px | 99,687 | 40,315 | 62,282 |     0.712 |     0.616 |
| cholec80 / YOLOv8s 클론    | 10 px | 96,916 | 30,699 | 65,053 | **0.759** | **0.598** |
| cholec80 / CLAD-Net        | 10 px | 91,700 | 46,628 | 70,269 |     0.663 |     0.566 |
| erop / **YOLO26 클론**     | 10 px | 37,648 |  8,358 | 13,070 |     0.818 |     0.742 |
| erop / 참조 구현           | 10 px | 37,816 |  7,994 | 12,902 |     0.826 |     0.746 |
| erop / YOLOv8s 클론        | 10 px | 38,488 |  7,914 | 12,230 | **0.829** | **0.759** |
| erop / CLAD-Net            | 10 px | 34,715 | 13,927 | 16,003 |     0.714 |     0.685 |

### 읽는 법

**정밀도에서 이긴다.** 잡은 팁의 오차 중앙값이 cholec80 3.45 px, erop 1.96 px로 스크래치
학습 모델 중 가장 작고, 루트 프로젝트의 어느 모델보다도 작다. 참조 구현(3.31 px, 1.90 px)에
0.06〜0.14 px 차이로 붙는다.

**놓치는 데서 진다.** 탐지 실패율이 cholec80 9.71 %로 비교 대상 중 CLAD-Net 다음으로 높고,
헝가리안 재현율도 가장 낮은 축이다. 팁을 잡기만 하면 정확히 잡지만, 잡지 못하는 팁이 더
많다는 뜻이다.

**히트맵 계열과는 성향이 다르다.** Hit@50 px에서 `gradient-seg`(cholec80 76.12 %)에 5.7 pp
뒤지고 P90 오차도 더 크다. 히트맵 모델은 "대충이라도 어딘가는 짚는" 성향이 있고, 탐지기는
"정확히 짚거나 아예 없거나"다. **정밀 좌표가 필요하면(로봇 제어, 계측) 이 모델이, 대략적
방향만 필요하면(카메라 유도) 놓침이 적은 `gradient-seg` 계열이 낫다.**

## 5. 오차 분포와 실패 모드

### 오차 거리 분포 (잡힌 팁 기준)

| 오차        | cholec80 (146,240개) | erop (47,697개) |
| ----------- | -------------------: | --------------: |
| 0〜5 px     |           **57.4 %** |      **73.3 %** |
| 5〜10 px    |                8.2 % |           5.6 % |
| 10〜20 px   |                5.8 % |           3.3 % |
| 20〜50 px   |                6.6 % |           3.8 % |
| 50〜100 px  |                5.0 % |           3.2 % |
| 100〜200 px |                8.0 % |           4.9 % |
| ≥ 200 px    |                9.0 % |           5.8 % |

**이봉형**이지만 첫 봉우리가 압도적이다. 절반 이상이 5 px 안에 들어오고, 나머지가 꼬리에
길게 퍼진다. 평균(46.12 px, 30.29 px)이 중앙값(3.45 px, 1.96 px)의 13〜16배인 이유가 이
꼬리다. **평균 거리로 이 모델을 평가하면 안 된다.**

### 큰 오차의 정체: 과소 탐지

같은 예측이 다른 GT 팁에도 최근접이었다면, 그 팁은 위치를 틀린 것이 아니라 그 프레임의
도구 하나를 통째로 놓친 것이다.

|                                               |            cholec80 |               erop |
| --------------------------------------------- | ------------------: | -----------------: |
| ≥ 200 px 오차 팁                              |              13,137 |              2,753 |
| 그중 **같은 예측을 다른 GT 팁과 공유**한 경우 | 11,454 (**87.2 %**) | 2,429 (**88.2 %**) |

**큰 오차의 87〜88 %는 위치 오류가 아니라 도구 하나를 통째로 놓친 것이다.** 이 비율은
참조 구현(85.4 %, 88.9 %), YOLOv8s 클론(88.5 %, 88.0 %), CLAD-Net, 그리고 루트 프로젝트가
자체 모델에서 보고하는 "공유 피크" 문제와 완전히 같은 성질이다. **end-to-end 헤드로 NMS를
없애도 이 실패 모드는 사라지지 않는다.** 중복 박스를 지우는 방식의 문제가 아니라,
한 프레임에 비슷하게 생긴 도구가 둘 이상 있을 때 그중 하나만 높은 점수를 받는 문제다.

### 프레임 내 도구 수에 따른 탐지 실패율

| 프레임의 GT 팁 수 |    cholec80 |        erop |
| ----------------- | ----------: | ----------: |
| 1개               | **19.25 %** | **13.87 %** |
| 2개               |      6.87 % |      3.55 % |
| 3개               |      5.93 % |      2.82 % |
| 4개 이상          |      8.39 % |      4.14 % |

**도구가 하나뿐인 프레임에서 가장 많이 놓친다.** 기구가 화면에 막 들어오거나 나가는 장면,
일부만 보이는 장면이 여기 몰리는 것으로 보인다. 앞 항목의 과소 탐지와는 별개의 실패 모드다.
팁 예측이 하나도 없는 프레임이 cholec80 12.03 %, erop 7.94 %이며, 참조 구현(9.52 %, 7.52 %)
보다 높다.

### 세션별 편차

| 데이터셋   | 세션 수 | Hit@10 px 최저              | 최고                        |        편차 |
| ---------- | ------: | --------------------------- | --------------------------- | ----------: |
| `cholec80` |      40 | video50 43.1 %              | video62 76.7 %              | **33.6 pp** |
| `erop`     |       5 | doctor_250620_084707 67.1 % | doctor_250702_101536 75.8 % |      8.7 pp |

cholec80의 편차가 크다. 영상 단위로 분리된 스플릿이라 test 영상의 촬영 조건·술기가 학습
영상과 다를 수 있고, **평균값 하나로 성능을 말하기 어렵다.** 최저·최고 세션이 참조 구현
(video50 44.5 %, video62 78.2 %)·YOLOv8s 클론(video50 44.6 %, video62 76.4 %)과 **완전히
같다.** 어려운 영상은 모델과 무관하게 어렵다는 뜻이며, 남은 오차의 상당 부분이 모델이
아니라 데이터 쪽에 있음을 시사한다.

### 신뢰도 분포

| 데이터셋   | 표본            |   p10 | 중앙값 |   p90 |  최대 |
| ---------- | --------------- | ----: | -----: | ----: | ----: |
| `cholec80` | 10 px 이내 예측 | 0.354 |  0.741 | 0.881 | 0.936 |
| `erop`     | 10 px 이내 예측 | 0.466 |  0.848 | 0.918 | 0.957 |

정확히 맞힌 예측이 0.94〜0.96까지 신뢰도를 낸다. YOLO26에도 objectness 항이 없어 클래스
점수가 곧 신뢰도이고, 게다가 end-to-end 헤드는 객체당 박스를 하나만 남기므로 중복 박스를
걸러 내던 NMS의 일을 점수 자체가 떠안는다. 그 상태에서도 0.9를 넘는 신뢰도가 나온다는 것은
one2one 분기가 의도대로 학습됐다는 신호다. (분포는 평가 임계값 `conf=0.25`에서 잘려 있다.)

## 6. 참조 구현과의 비교: 재구현 검증의 마지막 단계

[README](../README.md)의 `verify-clone`은 네 단계를 확인했다: 파라미터 수, `state_dict`
로드, **출력 재현**(최대 오차 0.000e+00), **손실 재현**(최대 오차 0.000e+00). 남은 질문
하나가 "그렇게 학습하면 실제로 같은 수준에 도달하는가"였고, 이 절이 그 답이다.

| 조건                            | YOLO26 클론                | 참조 구현 (`baseline/yolo26`) |
| ------------------------------- | -------------------------- | ----------------------------- |
| 아키텍처                        | 동일 (파라미터 9,949,412)  | 동일                          |
| 초기값                          | **무작위**                 | **COCO 사전학습**             |
| 학습률 스케줄                   | warmup 3 ep + **cosine**   | warmup + **linear**           |
| 그래디언트 누적                 | **없음**                   | nominal batch 64에 맞춰 누적  |
| 옵티마이저                      | MuSGD (직접 구현)          | `optimizer=auto` → MuSGD      |
| 에포크·배치·입력·팁 크기·stride | 150 / 16 / 640 / 32 px / 5 | 동일                          |

| 지표 (cholec80 test)  | YOLO26 클론 | 참조 구현 |     차이 |
| --------------------- | ----------: | --------: | -------: |
| mAP@0.5               |      0.7247 |    0.7348 |   −0.010 |
| mAP@0.5:0.95          |      0.5548 |    0.5644 |   −0.010 |
| tool AP@0.5           |      0.8172 |    0.8268 |   −0.010 |
| tip AP@0.5            |      0.6321 |    0.6427 |   −0.011 |
| 탐지 실패율           |      9.71 % |    7.45 % | +2.26 pp |
| Hit@10 px             |     59.27 % |   61.57 % | −2.30 pp |
| 팁 오차 중앙값        |     3.45 px |   3.31 px | +0.14 px |
| 헝가리안@10 px 정밀도 |   **0.728** |     0.712 |   +0.016 |
| 헝가리안@10 px 재현율 |       0.592 | **0.616** |   −0.023 |

| 지표 (erop test)      | YOLO26 클론 | 참조 구현 |     차이 |
| --------------------- | ----------: | --------: | -------: |
| mAP@0.5               |      0.8445 |    0.8476 |   −0.003 |
| mAP@0.5:0.95          |      0.6921 |    0.7011 |   −0.009 |
| tip AP@0.5            |      0.7996 |    0.8029 |   −0.003 |
| 탐지 실패율           |      5.96 % |    5.65 % | +0.31 pp |
| Hit@10 px             |     74.25 % |   74.59 % | −0.34 pp |
| 팁 오차 중앙값        |     1.96 px |   1.90 px | +0.06 px |
| 헝가리안@10 px 정밀도 |       0.818 |     0.826 |   −0.008 |
| 헝가리안@10 px 재현율 |       0.742 |     0.746 |   −0.004 |

**erop에서는 사실상 동률이다.** mAP@0.5 차이 0.003, Hit@10 px 차이 0.34 pp, 헝가리안 정밀도
차이 0.008로 전부 측정 잡음 수준이다. **cholec80에서는 1 %p 안쪽으로 뒤진다.** 두
데이터셋의 차이는 스플릿에서 온다: erop은 영상 내부를 섞어 나눈 스플릿이라 test 분포가
train과 가깝고, 사전학습이 채워 주는 "본 적 없는 장면에 대한 일반화"의 몫이 작다.
cholec80은 test 영상이 통째로 분리돼 있어 그 몫이 그대로 드러난다.

**격차의 방향이 한쪽으로 쏠려 있다.** 클론은 정밀도(0.728 대 0.712)에서 앞서고 재현율
(0.592 대 0.616)에서 뒤진다. 예측을 덜 낸다: cholec80 `tip` 예측 수가 2,625,798개로 참조
구현의 3,127,433개보다 16 % 적다. **클론이 못 배운 것은 "어디를 짚을지"가 아니라 "언제
짚을지"다.** 사전학습 특징이 채워 주는 것이 그 부분으로 보인다.

**결론: `ultralytics` 없이 YOLO26을 재현할 수 있음이 학습 결과로 확인됐다.** 출력·손실이
비트 단위로 같다는 것에 더해, 사전학습이라는 유리한 조건을 참조 구현에만 주고도 mAP 격차가
0.003〜0.011에 머문다.

## 7. YOLOv8s 클론과의 비교: 아키텍처 차이만 남긴 조건

두 클론은 **같은 학습 루프, 같은 증강, 같은 에포크·배치·입력, 같은 팁 크기, 같은
`--frame-stride`** 로 학습했고 둘 다 무작위 초기화에서 시작했다. 다른 것은 모델·손실·
할당·옵티마이저뿐이다.

| 지표 (cholec80 test)  | YOLO26 클론 | YOLOv8s 클론 |     차이 |
| --------------------- | ----------: | -----------: | -------: |
| mAP@0.5               |      0.7247 |   **0.7505** |   −0.026 |
| **mAP@0.5:0.95**      |  **0.5548** |       0.5517 |   +0.003 |
| tool AP@0.5           |      0.8172 |   **0.8348** |   −0.018 |
| tool AP@0.5:0.95      |      0.6948 |   **0.6956** |   −0.001 |
| tip AP@0.5            |      0.6321 |   **0.6662** |   −0.034 |
| **tip AP@0.5:0.95**   |  **0.4148** |       0.4078 |   +0.007 |
| 탐지 실패율           |      9.71 % |   **8.82 %** | +0.89 pp |
| Hit@10 px             |     59.27 % |  **59.87 %** | −0.60 pp |
| **팁 오차 중앙값**    | **3.45 px** |      3.60 px | −0.15 px |
| 헝가리안@10 px 정밀도 |       0.728 |    **0.759** |   −0.031 |

| 지표 (erop test)      | YOLO26 클론 | YOLOv8s 클론 |     차이 |
| --------------------- | ----------: | -----------: | -------: |
| mAP@0.5               |      0.8445 |   **0.8582** |   −0.014 |
| **mAP@0.5:0.95**      |  **0.6921** |       0.6854 |   +0.007 |
| tip AP@0.5            |      0.7996 |   **0.8213** |   −0.022 |
| **tip AP@0.5:0.95**   |  **0.5787** |       0.5673 |   +0.011 |
| 탐지 실패율           |      5.96 % |   **4.79 %** | +1.17 pp |
| Hit@10 px             |     74.25 % |  **75.91 %** | −1.66 pp |
| **팁 오차 중앙값**    | **1.96 px** |      2.04 px | −0.08 px |
| 헝가리안@10 px 정밀도 |       0.818 |    **0.829** |   −0.011 |

**두 데이터셋에서 방향이 완전히 같다: AP@0.5에서 지고 AP@0.5:0.95에서 이긴다.**
`tip` 클래스에서 그 대비가 가장 뚜렷하다 (cholec80 AP@0.5 −0.034 대 AP@0.5:0.95 +0.007).
팁 오차 중앙값도 두 데이터셋 모두 YOLO26 클론이 작다.

읽히는 대로 적으면 이렇다. **YOLO26 클론은 상자를 더 정확히 그리고, 더 적게 그린다.**

- **엄격한 IoU에서 이기는 이유**: DFL이 없어 `reg_max=1`이고, 세 번째 손실항이 네 거리의
  L1이다. 분포의 기댓값을 거쳐 좌표를 얻는 대신 값을 직접 맞추므로 이산 빈의 해상도 한계가
  없다. 팁 오차 중앙값이 작은 것도 같은 원인으로 보인다.
- **느슨한 IoU에서 지는 이유**: 후보를 덜 낸다. AP 곡선용 `conf=0.001`에서 `tip` 재현율이
  0.893으로 YOLOv8s 클론의 0.910에 못 미친다. AP@0.5는 곡선의 오른쪽 끝(재현율)에서
  결정되므로 여기서 손해를 본다. 탐지 실패율이 0.9〜1.2 pp 높은 것과 같은 현상이다.

**주의할 점 하나.** 이 비교에는 옵티마이저 차이(MuSGD 대 SGD)가 섞여 있다. `--optimizer sgd`
실행을 따로 쌓으면 그 몫을 떼어 낼 수 있으나, 이번 실험에서는 하지 않았다
([commands.md](commands.md) §4에 명령이 있다). 따라서 위 차이를 곧바로 "YOLO26 아키텍처의
성질"로 읽으면 안 되고, "YOLO26 설정 전체(모델 + 손실 + 할당 + 옵티마이저)의 성질"로
읽어야 한다.

**파라미터는 오히려 이쪽이 적다** (9.95 M 대 11.14 M, 89 %). 그럼에도 AP@0.5:0.95가 앞서고
팁 오차 중앙값이 작으니, 파라미터당 효율은 YOLO26 쪽이 낫다. 다만 실행 시간은 반대다(§8).

## 8. 속도

`eval-model`이 test 전수를 처리하며 측정한 프레임당 시간이다. letterbox, forward, 디코딩,
top-k를 포함한다(NMS는 없다).

| 데이터셋   | 장치   | YOLO26 클론 | YOLOv8s 클론 |     비 |
| ---------- | ------ | ----------: | -----------: | -----: |
| `cholec80` | cuda:2 |     9.81 ms |  **5.95 ms** | 1.65배 |
| `erop`     | cuda:3 |    10.38 ms |  **6.30 ms** | 1.65배 |

두 클론은 데이터셋마다 **같은 GPU**(RTX A6000)에서 측정됐으므로 이 비교는 성립한다.
CLAD-Net(cuda:0 10.11 ms)과 참조 구현(cuda:0 7.37 ms)은 다른 장치에서 측정됐으므로
같은 표에 넣지 않았다. 그 둘끼리는 같은 장치이므로 비교할 수 있다.

**NMS를 없앤 모델이 NMS를 도는 모델보다 1.65배 느리다.** 파라미터는 11 % 적은데도 그렇다.
어디에서 시간이 가는지 같은 장치에서 따로 쟀다.

> 아래 표는 `summary-results.md`에 없는 **보조 측정**이다. 측정 조건: RTX A6000 cuda:2,
> 배치 1, 640 × 640 무작위 입력, `torch.no_grad()`, 50회 warmup 후 300회 평균.
> letterbox와 호스트〜장치 전송을 뺐으므로 위 표보다 작게 나온다.

| 구간                                         |    시간 |
| -------------------------------------------- | ------: |
| YOLO26 클론: 백본 + 넥만                     | 5.87 ms |
| YOLO26 클론: 전체 (두 분기 + 디코딩 + top-k) | 9.70 ms |
| YOLO26 클론: one2many 분기를 빼면            | 8.24 ms |
| 그중 C2PSA 스테이지(레이어 10) 단독          | 0.56 ms |
| YOLOv8s 클론: 전체 (forward + 디코딩 + NMS)  | 5.15 ms |

두 가지가 드러난다.

1. **추론에서 쓰지 않는 분기를 계산하고 있다.** `Detect.forward`
   ([common/model.py:325](../common/model.py#L325))는 학습이든 추론이든 `one2many`와
   `one2one`을 **둘 다** 반환하는데, `Detector.detect`
   ([common/inference.py:115](../common/inference.py#L115))는 `["one2one"]`만 읽는다.
   버린 분기의 값이 **1.46 ms, 프레임당 시간의 15 %** 다. one2many 분기의 박스·클래스
   헤드는 P3(80 × 80)에서 가장 비싼데, 그 계산을 통째로 버리고 있다. 추론 경로에서 이
   분기를 건너뛰면 9.81 ms가 8.3 ms 근처로 내려간다. 참조 구현이 7.37 ms인 것과도
   방향이 맞는다.
2. **나머지 격차는 백본에 있고, C2PSA 때문이 아니다.** one2many를 빼고도 8.24 ms로
   YOLOv8s 클론 전체(5.15 ms)보다 느리다. YOLO26의 백본 + 넥만 해도 5.87 ms로 YOLOv8s의
   전체 forward보다 오래 걸린다. 눈에 띄는 후보였던 C2PSA 어텐션 스테이지는 0.56 ms에
   불과하다. 원인은 한 곳이 아니라 C3k2 블록 구성 전반에 퍼져 있다. **파라미터 수가
   실행 시간을 예측하지 못하는 전형적인 사례다.**

## 9. 결론과 다음 단계

### 결론

1. **`ultralytics` 없이 YOLO26을 재현할 수 있음이 학습 결과로 확인됐다.** 출력과 손실이
   참조와 비트 단위로 같다는 것에 더해, 사전학습이라는 유리한 조건을 참조에만 주고도
   mAP 격차가 cholec80 0.010, erop 0.003에 머문다.
2. **사전학습은 수렴 속도를 지배하고 최종 성능은 크게 바꾸지 않는다.** 참조 구현이
   1 에포크에 도달하는 지점에 클론은 20 에포크가 걸리지만, 100 에포크 뒤의 격차는 0.05,
   test 전수에서는 0.01이다. 남은 몫은 재현율에 몰려 있다.
3. **팁 좌표 산출기로 실용적인 수준이다.** 오차 중앙값 2.0〜3.5 px, Hit@10 px
   59〜74 %로 루트 프로젝트의 최고 모델보다 정밀하다.
4. **YOLOv8s 클론과는 강점이 갈린다.** AP@0.5·탐지 실패율은 YOLOv8s가, AP@0.5:0.95·팁
   오차 중앙값은 YOLO26이 앞선다. **end-to-end 헤드가 무조건 낫다는 결론은 이 데이터에서
   지지되지 않는다.**
5. **남은 최대 약점은 다중 도구 프레임의 과소 탐지다.** 큰 오차의 87〜88 %가 여기서
   나오며, NMS를 없앤 이 헤드에서도 비율이 그대로다. 다섯 접근 전부가 같은 문제를 안고 있다.

### 다음 단계 (효과가 클 것으로 보이는 순서)

1. **추론에서 one2many 분기 건너뛰기** ([§8](#8-속도)): 프레임당 15 %를 정확도 손실
   없이 회수한다. `Detect.forward`가 `self.training`일 때만 두 분기를 반환하게 하면 된다.
   가장 확실하고 가장 싸다.
2. **과소 탐지 대응**: 큰 오차의 87〜88 %를 차지한다. 이 헤드에는 낮출 NMS IoU가 없으므로
   assigner 쪽에서 손대야 한다. one2one 분기의 `topk`(현재 7)와 2차 `topk2`(현재 1)를
   올려 GT마다 양성을 늘리거나, 팁 클래스에 별도 임계값을 두는 방법을 시험할 수 있다.
3. **신뢰도 임계값 조정**: 이 모델의 약점은 재현율이고 강점은 정밀도인데, `conf=0.25`는
   형제 베이스라인과 맞춘 기본값일 뿐이다. `--conf`를 낮춰 가며 평가하면 탐지 실패율과
   오탐의 맞교환 곡선을 얻을 수 있고, 이 모델은 그 곡선에서 얻을 것이 많은 쪽에 있다.
4. **에포크를 110〜125로**: §2에서 과적합 시작점을 확인했다. 학습 시간을 5.8 h → 4.3 h로
   줄이면서 같은 성능을 얻는다.
5. **MuSGD의 몫 분리**: §7의 비교에는 옵티마이저 차이가 섞여 있다. `--optimizer sgd`
   실행을 한 회차 쌓으면 아키텍처 몫과 옵티마이저 몫을 나눌 수 있다.
   명령은 [commands.md](commands.md) §4에 있다.
6. **입력 캔버스 모양**: 640 × 640 letterbox는 34.8 %가 패딩이다. 800 × 512로 바꾸면 같은
   연산량에서 물체가 23 % 커진다
   ([CLAD-Net 보고서 §8](../../cladnet/docs/experimental-results.md)).

## 재현 명령

```bash
# 학습 (완료됨: data/model/<dataset>/에 산출물이 있다)
./baseline/yolo26clone/run train-model --dataset cholec80 --frame-stride 5 --val-frames 1500 --workers 12 --device cuda:2
./baseline/yolo26clone/run train-model --dataset erop     --frame-stride 5 --val-frames 1500 --workers 12 --device cuda:3

# 평가 (완료됨: data/results/<dataset>/test/에 산출물이 있다)
./baseline/yolo26clone/run eval-model --dataset cholec80 --split test
./baseline/yolo26clone/run eval-model --dataset erop     --split test

# 이 보고서가 인용하는 수치 표 재생성
./baseline/yolo26clone/run generate-summary

# 구현이 참조와 일치하는지 다시 확인 (§6)
uv run --project baseline/yolo26 python baseline/yolo26clone/scripts/export-reference.py
./baseline/yolo26clone/run verify-clone
```

구조 설명은 [README](../README.md), 인수 설명과 추가 실험 명령은 [commands.md](commands.md),
비교 대상의 상세는 [참조 구현 보고서](../../yolo26/docs/experimental-results.md)·
[YOLOv8s 클론 보고서](../../yolov8sclone/docs/experimental-results.md)·
[CLAD-Net 보고서](../../cladnet/docs/experimental-results.md)를 참고한다.
